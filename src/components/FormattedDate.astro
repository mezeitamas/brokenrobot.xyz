---
import { SITE_METADATA } from '../consts';

type Props = {
    date: Date;
};

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
    {
        date.toLocaleDateString(SITE_METADATA.LANGUAGE, {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
        })
    }
</time>
