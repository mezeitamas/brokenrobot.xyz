---
import type { ImageMetadata } from 'astro';
import { Picture } from 'astro:assets';

type Props = {
    src: ImageMetadata;
    alt: string;
};

const { src, alt } = Astro.props;
---

<Picture
    src={src}
    alt={alt}
    layout="full-width"
/>
