apiVersion: v1
kind: Service
metadata:
  name: brokenrobot-xyz-service
  labels:
    app: brokenrobot-xyz
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: brokenrobot-xyz
